class Solution(object):
    def addDigits(self, num):
        """
        :type num: int
        :rtype: int
        """
        while num >=10:

            num_str = str(num)
            digit_sum = sum(int(digit) for digit in num_str)
            num = digit_sum
            
        return num
