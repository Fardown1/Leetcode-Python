class Solution(object):
    def uniquePaths(self, m, n):
        """
        :type m: int
        :type n: int
        :rtype: int
        """
        import math

        ans = factorial(m+n-2)/(factorial(m-1)*factorial(n-1))
        return ans
